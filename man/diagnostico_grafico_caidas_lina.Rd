% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/completitud_lina.R
\name{diagnostico_grafico_caidas_lina}
\alias{diagnostico_grafico_caidas_lina}
\title{Diagnostico grafico de caidas del criterio Lina}
\usage{
diagnostico_grafico_caidas_lina(
  res_lina = NULL,
  dfs = NULL,
  plot = TRUE,
  main = "Caidas criterio Lina por nivel y razon",
  cex.names = 0.8,
  las = 2,
  ...
)
}
\arguments{
\item{res_lina}{Resultado de \code{diagnostico_completitud_lina()}. Si es \code{NULL},
la funcion lo calcula internamente a partir de \code{dfs}.}

\item{dfs}{Lista nombrada de data frames por capitulo. Se usa solo si
\code{res_lina} es \code{NULL}.}

\item{plot}{Si es \code{TRUE}, construye el grafico interactivo con \code{plotly}.}

\item{main}{Titulo principal del grafico.}

\item{cex.names}{Argumento conservado por compatibilidad. No se usa en la
version interactiva.}

\item{las}{Argumento conservado por compatibilidad. No se usa en la version
interactiva.}

\item{...}{Argumentos adicionales que se pasan a
\code{diagnostico_completitud_lina()} cuando \code{res_lina} es \code{NULL}.}
}
\value{
Lista con:
\itemize{
\item \code{resumen_totales}: total de caidas por vivienda, hogar y persona
a nivel persona.
\item \code{resumen_razones}: razones de caida por nivel, tambien a nivel
persona.
\item \code{datos_grafico}: tabla usada para construir el grafico.
\item \code{grafico}: objeto \code{plotly} si \code{plot = TRUE}; en otro caso
\code{NULL}.
}
}
\description{
Construye un resumen de caidas del criterio Lina y un grafico de barras
basado en la misma unidad analitica de personas. Para vivienda y hogar, las
razones de caida se propagan al nivel persona para que el total y sus
desagregaciones sean comparables con \code{encuestas_caidas_lina}.
}
