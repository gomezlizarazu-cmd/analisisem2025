% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/incompletitud_tematica.R
\name{diagnostico_incompletitud_tematica}
\alias{diagnostico_incompletitud_tematica}
\title{Diagnosticar incompletitud tematica}
\usage{
diagnostico_incompletitud_tematica(
  dfs,
  reglas_variables = NULL,
  diag_tres = NULL,
  ruta_exportacion = NULL,
  base_hogar_cap = "C",
  base_persona_cap = "E",
  edad_var = "NPCEP4"
)
}
\arguments{
\item{dfs}{Lista nombrada de capitulos.}

\item{reglas_variables}{Tabla de reglas tematicas. Debe contener como minimo
las columnas \code{capitulo}, \code{nivel}, \code{variable} y \code{descripcion}. Si es
\code{NULL}, usa una regla por defecto para \code{NVCBP11AA}.}

\item{diag_tres}{Resultado de \code{diagnostico_caidas_tres_criterios()}. Si es
\code{NULL}, se calcula internamente.}

\item{ruta_exportacion}{Ruta opcional para exportar un Excel.}

\item{base_hogar_cap}{Capitulo base del universo hogar si \code{diag_tres} es
\code{NULL}.}

\item{base_persona_cap}{Capitulo base del universo persona si \code{diag_tres} es
\code{NULL}.}

\item{edad_var}{Variable de edad si \code{diag_tres} es \code{NULL}.}
}
\value{
Una lista con:
\describe{
\item{detalle_tematica_base}{Detalle de fallas en el nivel natural de cada
regla.}
\item{detalle_tematica_propagado}{Detalle expandido con propagacion
descendente y agregacion ascendente.}
\item{resumen_tematica}{Resumen de afectacion por regla y nivel resultado.}
\item{solo_tematica}{Casos que fallan por tematica pero no por los tres
criterios actuales.}
\item{cruce_con_tres_criterios}{Cruce del detalle tematico propagado con
\code{diag_tres}.}
\item{diag_tres}{Salida usada de \code{diagnostico_caidas_tres_criterios()}.}
\item{archivo}{Ruta del Excel exportado, si aplica.}
}
}
\description{
Evalua faltantes en variables tematicamente criticas y construye un
diagnostico jerarquico respetando los niveles de la encuesta:
vivienda (\code{DIRECTORIO}), hogar (\code{DIRECTORIO + SECUENCIA_P}) y persona
(\code{DIRECTORIO + SECUENCIA_P + ORDEN}).
}
\details{
Esta funcion no reemplaza la metodologia oficial implementada en
\code{diagnostico_caidas_tres_criterios()}. En cambio, agrega una capa adicional
de evaluacion sobre variables tematicas faltantes, con propagacion
descendente y agregacion ascendente segun el nivel natural de la regla.

Si una regla tematica es de nivel vivienda, la caida se propaga hacia hogar
y persona. Si es de nivel hogar, se propaga hacia persona y se agrega a
vivienda al resumir. Si es de nivel persona, afecta directamente a persona y
se agrega a hogar y vivienda al resumir.
}
\examples{
\dontrun{
diag_tematica <- diagnostico_incompletitud_tematica(dfs)
diag_tematica$resumen_tematica
}

}
