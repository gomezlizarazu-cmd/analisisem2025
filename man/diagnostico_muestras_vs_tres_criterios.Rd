% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/muestras_tres_criterios.R
\name{diagnostico_muestras_vs_tres_criterios}
\alias{diagnostico_muestras_vs_tres_criterios}
\title{Cruzar archivo de Muestras con el consolidado final de tres criterios}
\usage{
diagnostico_muestras_vs_tres_criterios(
  archivo_muestras,
  diag_tres = NULL,
  dfs = NULL,
  base_hogar_cap = "C",
  base_persona_cap = "E",
  edad_var = "NPCEP4",
  cap_hog = "C",
  ruta_exportacion = NULL,
  incluir_diag_tres = FALSE
)
}
\arguments{
\item{archivo_muestras}{Ruta del archivo Excel enviado por Muestras.}

\item{diag_tres}{Resultado de \code{diagnostico_caidas_tres_criterios()}. Si es
\code{NULL}, se calcula internamente a partir de \code{dfs}.}

\item{dfs}{Lista nombrada de capítulos. Se usa solo cuando \code{diag_tres} es
\code{NULL} o cuando se requiere apoyar el mapeo de llaves faltantes.}

\item{base_hogar_cap}{Capítulo base del universo hogar cuando \code{diag_tres} es
\code{NULL}.}

\item{base_persona_cap}{Capítulo base del universo persona cuando \code{diag_tres}
es \code{NULL}.}

\item{edad_var}{Variable de edad para \code{diagnostico_caidas_tres_criterios()}
cuando \code{diag_tres} es \code{NULL}.}

\item{cap_hog}{Capítulo de hogar usado para apoyar el mapeo de
\code{DIRECTORIO_HOG} hacia \code{SECUENCIA_P}. Por defecto \code{"C"}.}

\item{ruta_exportacion}{Ruta opcional para exportar el cruce a un archivo
Excel. Si es \code{NULL}, no exporta. Si se suministra, genera un \code{.xlsx} con
las hojas \code{resumen}, \code{detalle}, \code{detectadas}, \code{no_detectadas} y
\code{reporte_final_match}, todas basadas en la comparación oficial contra
\code{reporte_final_caidas}.}

\item{incluir_diag_tres}{Si es \code{TRUE}, incluye \code{diag_tres} dentro de la
salida para facilitar trazabilidad.}
}
\value{
Una lista con:
\describe{
\item{problemas_muestras}{Base unificada de casos reportados por Muestras,
con nivel, variable reportada y llaves analíticas normalizadas.}
\item{cruce_detalle}{Cruce caso a caso entre Muestras, \code{reporte_final_caidas}
y las bases intermedias de \code{diagnostico_caidas_tres_criterios()}, con
columnas separadas para cruce estricto por nivel y cruce flexible por
\code{DIRECTORIO}.}
\item{cruce_resumen}{Resumen por hoja/problema con detección final,
detección intermedia y conteos por criterio.}
\item{casos_detectados}{Casos reportados por Muestras que sí quedaron
identificados en \code{reporte_final_caidas}.}
\item{no_detectadas}{Casos reportados por Muestras que no quedaron
identificados en \code{reporte_final_caidas}.}
\item{archivo}{Ruta normalizada del Excel exportado cuando
\code{ruta_exportacion} no es \code{NULL}.}
\item{diag_tres}{Salida original de \code{diagnostico_caidas_tres_criterios()}
cuando \code{incluir_diag_tres = TRUE}.}
}
}
\description{
Lee un archivo Excel de Muestras con listados de valores faltantes en
variables clave, identifica el nivel analítico afectado (vivienda, hogar o
persona) y cruza esos casos contra la salida de
\code{diagnostico_caidas_tres_criterios()}.
}
\details{
La comparación oficial se realiza contra \code{reporte_final_caidas}, que es el
consolidado final de encuestas caídas de la metodología y que además incluye
posibles duplicados de personas (\code{cae_duplicado}). De manera complementaria,
la función también puede devolver una lectura intermedia basada en
\code{viviendas_eval}, \code{hogares_eval} y \code{personas_eval} para entender cómo venía
cayendo cada caso antes de la consolidación final.

La pregunta principal que responde esta función es:
“De los casos reportados por Muestras, ¿cuáles sí quedaron identificados en
el consolidado final \code{reporte_final_caidas} de la metodología?”

La detección final se evalúa en dos niveles. Primero intenta un cruce
estricto contra una base derivada de \code{reporte_final_caidas} en el nivel
correspondiente: \code{reporte_vivienda}, \code{reporte_hogar} o \code{reporte_persona}.
Segundo, aplica un criterio adicional de detección por \code{DIRECTORIO} usando
una base resumida por encuesta construida desde \code{reporte_final_caidas}.

En consecuencia, un caso puede considerarse detectado aunque no cruce al
nivel exacto reportado por Muestras, siempre que su \code{DIRECTORIO} esté
incluido en \code{reporte_final_caidas}. En esos casos, la función anexa al
detalle el diagnóstico resumido disponible para ese \code{DIRECTORIO}.

Adicionalmente, la función valida si el \code{DIRECTORIO} reportado por Muestras
existe en la base actual \code{dfs}. Esto permite distinguir entre casos que no
fueron detectados porque ya no existen en la base actual y casos que sí
existen, pero no aparecen en \code{reporte_final_caidas}.

El orden de evaluaciÃ³n es explÃ­cito: primero se verifica si el
\code{DIRECTORIO} existe en la base actual \code{dfs}; solo despuÃ©s se evalÃºa si
aparece o no en \code{reporte_final_caidas}.

\code{match_estricto_nivel} refleja únicamente el join estricto por nivel y
\code{match_directorio_reporte_final} refleja la inclusión del \code{DIRECTORIO} en
\code{reporte_final_caidas}. \code{detectada_final} se define como la unión lógica de
ambos y \code{match_en_reporte_final} se conserva como alias de esa detección
final para facilitar trazabilidad. Ninguno depende de los valores de
\code{cae_existencia}, \code{cae_lina}, \code{cae_campo}, \code{cae_duplicado} ni de
\code{n_criterios_reporte}.
}
\examples{
\dontrun{
diag_tres <- diagnostico_caidas_tres_criterios(dfs)

cruce <- diagnostico_muestras_vs_tres_criterios(
  archivo_muestras = "tablas_na.xlsx",
  diag_tres = diag_tres,
  dfs = dfs,
  ruta_exportacion = "cruce_muestras_vs_tres_criterios.xlsx"
)

cruce$cruce_resumen
cruce$no_detectadas
}

}
