% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/em_completa.R
\name{verificar_universos_em_completa_diag}
\alias{verificar_universos_em_completa_diag}
\title{Auditar coherencia diagnóstica entre \code{reporte_final_caidas} y el capítulo E}
\usage{
verificar_universos_em_completa_diag(
  diag = diag_con_tematica,
  dfs_original = NULL,
  cap_persona = "E"
)
}
\arguments{
\item{diag}{Objeto diagnóstico de entrada. Debe ser una lista y contener
\code{reporte_final_caidas}. Por defecto usa \code{diag_con_tematica} si existe en
el entorno de llamada.}

\item{dfs_original}{Lista original de capítulos. Si es \code{NULL}, la función
intenta usar \code{diag$dfs}.}

\item{cap_persona}{Capítulo físico de personas que se quiere auditar. Por
defecto \code{"E"}.}
}
\value{
Una lista con:
\describe{
\item{resumen}{Tibble ejecutivo con el total diagnóstico, observadas en E,
no observadas en E y sus porcentajes.}
\item{hallazgo_principal}{Texto resumido de auditoría.}
\item{patrones_propagacion}{Conteo de combinaciones de variables de caída
para los casos no observados en E.}
\item{criterios_fuera_E}{Conteo de \code{criterio_principal_reporte} para los
casos no observados en E, si existe en el reporte.}
\item{detalle}{Lista con \code{personas_diag},
\code{personas_observadas_en_E} y \code{personas_no_observadas_en_E}.}
}
}
\description{
Contrasta el universo diagnóstico consolidado en \code{reporte_final_caidas}
contra la presencia física de registros persona en el capítulo definido por
\code{cap_persona}.
}
\details{
Esta función audita la coherencia entre el universo diagnóstico consolidado
en \code{reporte_final_caidas} y la presencia física de registros persona en el
capítulo E, permitiendo distinguir entre caídas observadas y propagaciones
diagnósticas desde niveles superiores.

La función no reconstruye caídas ni usa \code{construir_base_em_completa()}. Su
propósito es comparar directamente:
\itemize{
\item el universo persona del diagnóstico final;
\item el universo persona observado físicamente en \code{dfs_original[[cap_persona]]}.
}
}
\examples{
\dontrun{
verif_diag <- verificar_universos_em_completa_diag(
  diag = diag_con_tematica,
  dfs_original = dfs
)

verif_diag$resumen
verif_diag$hallazgo_principal
verif_diag$patrones_propagacion
}

}
