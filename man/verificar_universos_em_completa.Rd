% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/em_completa.R
\name{verificar_universos_em_completa}
\alias{verificar_universos_em_completa}
\title{Verificar coherencia de universos en la base de encuestas completas}
\usage{
verificar_universos_em_completa(
  dfs_original,
  dfs_completa,
  directorios_excluidos,
  cap_vivienda = "A",
  cap_hogar = "E",
  cap_persona = "E"
)
}
\arguments{
\item{dfs_original}{Lista original de capitulos antes del filtrado.}

\item{dfs_completa}{Lista de capitulos ya filtrados a encuestas completas.}

\item{directorios_excluidos}{Vector de \code{DIRECTORIO} excluidos al construir la
base completa.}

\item{cap_vivienda}{Capitulo que se usara para construir el universo de
vivienda. Por defecto \code{"A"}.}

\item{cap_hogar}{Capitulo que se usara para construir el universo de hogar.
Por defecto \code{"E"}.}

\item{cap_persona}{Capitulo que se usara para construir el universo de
persona. Por defecto \code{"E"}.}
}
\value{
Una lista con:
\describe{
\item{resumen}{Tibble resumen por nivel con los conteos de conservacion.}
\item{check_general}{\code{TRUE} si todos los niveles pasan las validaciones de
conservacion e interseccion; \code{FALSE} en caso contrario.}
\item{problemas}{Tibble con los niveles que no pasan alguna validacion.}
\item{detalle}{Lista con el detalle de vivienda, hogar y persona.}
}
}
\description{
Audita que la base construida con \code{construir_base_em_completa()} conserve la
identidad de universos a nivel de vivienda, hogar y persona frente a la base
original.
}
\details{
Esta funcion no reconstruye las caidas. Su proposito es auditar que la base
completa construida desde \code{construir_base_em_completa()} sea coherente con
los universos originales a nivel de vivienda, hogar y persona.

La validacion contrasta, para cada nivel, que:

\code{universo_original = universo_completo + universo_excluido}

ademas de verificar que ningun \code{DIRECTORIO} excluido permanezca en la base
completa y que no existan llaves compartidas simultaneamente entre el
universo completo y el universo excluido.
}
\examples{
\dontrun{
em_completa <- construir_base_em_completa(
  dfs = dfs,
  reporte_final_caidas = diag_con_tematica$reporte_final_caidas
)

verificacion_em <- verificar_universos_em_completa(
  dfs_original = dfs,
  dfs_completa = em_completa$dfs,
  directorios_excluidos = em_completa$directorios_excluidos
)

verificacion_em$resumen
verificacion_em$problemas
}

}
