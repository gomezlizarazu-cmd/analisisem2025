% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/robot_inspeccion_segmentos.R
\name{robot_inspeccion_segmentos}
\alias{robot_inspeccion_segmentos}
\title{Robot de inspeccion para segmentos completos}
\usage{
robot_inspeccion_segmentos(
  dfs,
  segmentos,
  municipios = NULL,
  salida_segmentos = NULL,
  carpeta_salida = NULL,
  fecha_corte = NULL,
  exportar_excel = TRUE,
  exportar_robots_individuales = FALSE,
  incluir_capitulos = NULL,
  archivo = NULL,
  sobrescribir = TRUE
)
}
\arguments{
\item{dfs}{Lista nombrada de data frames por capitulo.}

\item{segmentos}{Vector de segmentos a inspeccionar. Puede contener segmentos
completos tipo \code{CODMPIO_SEGMENTO} o segmentos cortos; los segmentos cortos
pueden ser ambiguos si \code{municipios = NULL}.}

\item{municipios}{Vector opcional de municipios/codigos para desambiguar
segmentos cortos, o diccionario con columnas \code{cod_mpio} y \code{municipio}.}

\item{salida_segmentos}{Objeto opcional devuelto por
\code{reporte_segmentos_cascada_muestras()}.}

\item{carpeta_salida}{Carpeta de salida para el Excel consolidado y, si
aplica, los robots individuales.}

\item{fecha_corte}{Fecha o etiqueta de corte para nombrar archivos. Si es
\code{NULL}, usa la fecha del sistema en formato \code{YYYYMMDD}.}

\item{exportar_excel}{Si \code{TRUE}, exporta el Excel consolidado.}

\item{exportar_robots_individuales}{Si \code{TRUE}, ejecuta
\code{robot_inspeccion_encuesta()} para cada \code{DIRECTORIO} encontrado.}

\item{incluir_capitulos}{Vector opcional de capitulos a incluir. Si es
\code{NULL}, incluye todos los capitulos disponibles en \code{dfs}.}

\item{archivo}{Ruta opcional del Excel consolidado. Si es \code{NULL}, se crea en
\code{carpeta_salida}.}

\item{sobrescribir}{Si \code{TRUE}, permite reemplazar el Excel consolidado.}
}
\value{
Lista con segmentos solicitados/encontrados/no encontrados,
directorios, sabana por capitulo y rutas exportadas.
}
\description{
Exporta una sabana consolidada con todos los capitulos filtrados a los
\code{DIRECTORIO} pertenecientes a una lista de segmentos. La seleccion de
viviendas se hace desde el capitulo \code{A}, usando \code{SEGMENTO} y \code{DIRECTORIO}.
}
\details{
La funcion no recalcula diagnosticos ni modifica la logica de
\code{robot_inspeccion_encuesta()}. Si se entrega una salida previa de
\code{reporte_segmentos_cascada_muestras()}, agrega sus tablas principales al
Excel consolidado.
}
\examples{
\dontrun{
segmentos_problema <- c(
  "25898_17183", "25898_17189", "25898_17191", "25898_17207",
  "25473_30303", "25473_30305", "25473_30324", "25473_30328",
  "25754_30439"
)

robot_seg <- robot_inspeccion_segmentos(
  dfs = dfs,
  segmentos = segmentos_problema,
  salida_segmentos = salida_segmentos,
  carpeta_salida = carpeta_salida,
  fecha_corte = fecha_corte,
  exportar_excel = TRUE,
  exportar_robots_individuales = FALSE
)

robot_seg$directorios_segmentos
robot_seg$archivo_excel
}

}
