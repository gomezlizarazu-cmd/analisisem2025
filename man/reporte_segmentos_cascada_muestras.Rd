% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reporte_segmentos_cascada_muestras.R
\name{reporte_segmentos_cascada_muestras}
\alias{reporte_segmentos_cascada_muestras}
\title{Reporte de cascada para segmentos problematicos de Muestras}
\usage{
reporte_segmentos_cascada_muestras(
  segmentos,
  dfs = NULL,
  diag_tres = NULL,
  diag_con_tematica = NULL,
  em_completa = NULL,
  carpeta_salida = NULL,
  fecha_corte = NULL,
  reglas_tematica = NULL,
  exportar = !is.null(carpeta_salida),
  formato_exportacion = c("xlsx", "csv")
)
}
\arguments{
\item{segmentos}{Vector de segmentos a reportar.}

\item{dfs}{Lista de capitulos de la Encuesta Multiproposito. Debe incluir el
capitulo \code{A} con \code{DIRECTORIO} y \code{SEGMENTO}; idealmente tambien \code{E}.}

\item{diag_tres}{Resultado de \code{diagnostico_caidas_tres_criterios()}.}

\item{diag_con_tematica}{Resultado opcional de
\code{diagnostico_caidas_con_tematica()}. Si se suministra, se usa como fuente
del escenario con reglas tematicas.}

\item{em_completa}{Objeto opcional que puede contener \code{dfs}, \code{diag_tres} o
\code{diag_con_tematica}. Se usa solo como contenedor de insumos si esos
argumentos no se entregan directamente.}

\item{carpeta_salida}{Carpeta donde se exportan los archivos si
\code{exportar = TRUE}.}

\item{fecha_corte}{Fecha o etiqueta de corte para nombrar archivos. Si es
\code{NULL}, usa la fecha del sistema.}

\item{reglas_tematica}{Reglas tematicas opcionales, usadas solo para
documentar variables de la tabla de criterios reutilizada por el reporte.}

\item{exportar}{Si \code{TRUE}, exporta las tablas a \code{carpeta_salida}.}

\item{formato_exportacion}{Formato de exportacion: \code{"xlsx"} o \code{"csv"}.}
}
\value{
Lista con:
\describe{
\item{resumen_segmento}{Resumen por segmento a nivel \code{DIRECTORIO}.}
\item{cascada_detallada_segmento}{Cascada larga por segmento y etapa a nivel \code{DIRECTORIO}.}
\item{detalle_caidas}{Detalle de filas/personas asociadas a viviendas caidas.}
\item{resumen_detalle_segmento}{Resumen de filas y llaves unicas del detalle.}
\item{personas_asociadas_segmento}{Conteos observados en capitulo \code{E} para viviendas caidas.}
\item{comparacion_con_sin_tematica}{Comparacion con y sin reglas tematicas a nivel \code{DIRECTORIO}.}
\item{segmentos_no_encontrados}{Segmentos solicitados sin registros en la base.}
\item{archivos_exportados}{Rutas exportadas, o \code{NULL} si no se exporta.}
}
}
\description{
Construye una salida focalizada para segmentos enviados por Muestras, sin
recalcular diagnosticos ni modificar los criterios generales de caida. La
funcion toma los diagnosticos ya calculados y resume, a nivel vivienda
(\code{DIRECTORIO}), la cascada excluyente de caidas por completitud estructural,
campo, Lina, duplicados y tematica.
}
\examples{
\dontrun{
devtools::load_all("C:/Users/gomez/OneDrive/Documentos/analisisem2025")

segmentos_problema <- c(
  "17183", "17189", "17191", "17207",
  "30303", "30305", "30324", "30328", "30439"
)

salida_segmentos <- reporte_segmentos_cascada_muestras(
  segmentos = segmentos_problema,
  dfs = dfs,
  diag_tres = diag_tres,
  diag_con_tematica = diag_con_tematica,
  carpeta_salida = "outputs/muestras_completitud",
  fecha_corte = fecha_corte
)

salida_segmentos$resumen_segmento
salida_segmentos$cascada_detallada_segmento
salida_segmentos$comparacion_con_sin_tematica
}

}
