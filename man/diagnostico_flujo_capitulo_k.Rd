% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostico_flujo_capitulo_k.R
\name{diagnostico_flujo_capitulo_k}
\alias{diagnostico_flujo_capitulo_k}
\title{Diagnosticar perdida de informacion en el flujo del Capitulo K}
\usage{
diagnostico_flujo_capitulo_k(
  dfs,
  diccionario = NULL,
  variable_k23_final = "NPCKP17",
  vars_cap_k = NULL,
  detener_si_duplicados = TRUE,
  incluir_texto_libre = FALSE
)
}
\arguments{
\item{dfs}{Lista nombrada de capitulos. Debe incluir \code{K}; si incluye \code{E},
la edad se toma preferiblemente desde ese capitulo por llave persona.}

\item{diccionario}{Diccionario opcional del Capitulo K. Si se suministra,
se usa para enriquecer reglas y para calcular un resumen agregado con
\code{\link[=resumir_flujo_capitulo_k]{resumir_flujo_capitulo_k()}}.}

\item{variable_k23_final}{Variable final/imputada de posicion ocupacional.
Por defecto \code{"NPCKP17"}. Se usa como verdad para reconstruir el flujo
posterior y no se marca como candidata a imputacion.}

\item{vars_cap_k}{Vector opcional de variables de K a evaluar. Si es \code{NULL},
se evaluan las variables incluidas en las reglas implementadas.}

\item{detener_si_duplicados}{Logico. Si \code{TRUE}, la funcion se detiene cuando
K no es unico por \code{DIRECTORIO + SECUENCIA_P + ORDEN}.}

\item{incluir_texto_libre}{Logico reservado para ampliaciones del diccionario
operativo. En esta version se conservan todas las variables normadas.}
}
\value{
Lista con \code{diagnostico_persona_variable}, \code{resumen_variables},
\code{resumen_personas}, \code{resumen_bloques},
\code{variables_candidatas_imputacion}, \code{auditoria_llaves}, \code{reglas_flujo},
\code{auditoria_por_pregunta}, \code{variables_ausentes}, \code{duplicados_k} y
\code{resumen_flujo_agregado}.
}
\description{
Construye un diagnostico persona-variable del Capitulo K, separando vacios
esperados por salto valido de vacios criticos donde la persona debia
responder segun el flujo teorico. La unidad de analisis es siempre persona:
\code{DIRECTORIO + SECUENCIA_P + ORDEN}.
}
\details{
\code{NPCKP17} corresponde a K23. En la base del 22 de junio esta variable ya
viene imputada, por lo que se crea \code{NPCKP17_FINAL = NPCKP17} y se usa ese
valor como insumo cierto para el flujo posterior. La pregunta para variables
posteriores a K23 es: dada la posicion ocupacional final, esta variable
debia responderse?
}
\examples{
\dontrun{
diag_k <- diagnostico_flujo_capitulo_k(dfs = dfs, diccionario = reglas_k_total)
diag_k$resumen_variables
diag_k$variables_candidatas_imputacion
}

}
