% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/em_completa.R
\name{construir_base_em_completa}
\alias{construir_base_em_completa}
\title{Construir la base definitiva de encuestas completas}
\usage{
construir_base_em_completa(dfs, reporte_final_caidas)
}
\arguments{
\item{dfs}{Lista de data frames con los capitulos de la encuesta.}

\item{reporte_final_caidas}{Data frame consolidado con las variables de
caida por encuesta/persona.}
}
\value{
Una lista con:
\describe{
\item{dfs}{Lista de capitulos filtrados a la base de encuestas completas.}
\item{resumen}{Tibble con el numero de registros antes, despues y
excluidos por capitulo.}
\item{directorios_excluidos}{Vector de \code{DIRECTORIO} excluidos por tener al
menos una caida.}
\item{n_encuestas_excluidas}{Numero total de directorios excluidos.}
}
}
\description{
Filtra todos los capitulos de \code{dfs} para conservar unicamente las encuestas
cuyo \code{DIRECTORIO} no presenta caidas en los criterios integrados disponibles
dentro de \code{reporte_final_caidas}.
}
\details{
Una encuesta completa se define como aquella cuyo \code{DIRECTORIO} no presenta
ninguna caida en los criterios integrados disponibles dentro de
\code{reporte_final_caidas}. La funcion detecta automaticamente cuales variables
de caida estan presentes en el reporte (\code{cae_existencia}, \code{cae_lina},
\code{cae_campo}, \code{cae_duplicado}, \code{cae_tematica}), resume la señal a nivel de
encuesta (\code{DIRECTORIO}) y elimina ese conjunto de directorios de todos los
capitulos de \code{dfs}.

La salida mantiene la estructura de \code{dfs}, pero filtrada a la base oficial
de encuestas completas, e incluye un resumen de registros antes y despues
del filtrado por capitulo.
}
\examples{
\dontrun{
em_completa <- construir_base_em_completa(
  dfs = dfs,
  reporte_final_caidas = diag_con_tematica$reporte_final_caidas
)

names(em_completa$dfs)
em_completa$resumen

em_completa_tres <- construir_base_em_completa(
  dfs = dfs,
  reporte_final_caidas = diag_tres$reporte_final_caidas
)
}

}
